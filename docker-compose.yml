version: "3.9"
services:
  front:
    build: frontend/.
    container_name: frontend_lists_container
    ports:
      - "3001:3001"
    networks:
      - postgres2
  back:
    build: backend/.
    container_name: backend_lists_container
    ports:
      - "3002:3002"
    command: npm start
    depends_on:
      - postgres
    networks:
      - postgres2
  postgres:
    container_name: postgres_container_lists
    image: postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-docker}
      PGDATA: /data/postgres
    volumes:
       - ./postgres:/data/postgres
    ports:
      - "5433:5432"
    networks:
      - postgres2
    restart: unless-stopped

networks:
  postgres2:
